version: '3.8'

services:
  tsc-editor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tsc-editor
    ports:
      - "3000:80"
    restart: unless-stopped
    labels:
      - "description=TSC Editor - Disponible en http://localhost:3000"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
    command: >
      sh -c "echo '\n🚀 TSC Editor iniciado correctamente!' &&
      echo '📱 Accede a la aplicación en: http://localhost:3000' &&
      nginx -g 'daemon off;'"
